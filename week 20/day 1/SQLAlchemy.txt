SQLalchemy has different layers 

1. PostgreSQL layer 

SELECT * FROM menu_items;



2. SQLAlchemy session/execute syntax 

result = db.execute(text("SELECT * FROM menu_items"))


3. Result processing Syntax 
this is python handling 


items = result.mappings().all()

“Turn the returned rows into dictionary-like rows and give me all of them.”

[
    {"id": 1, "name": "Beer", "price_yen": 600},
    {"id": 2, "name": "Pizza", "price_yen": 1200},
]




4. ORM syntax 
we will do this later 

items = db.scalars(select(MenuItem)).all()