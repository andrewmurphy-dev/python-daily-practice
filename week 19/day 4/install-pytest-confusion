how do we install pytest !???

this is very important ! 


also another confusion when i clone a repo , i knew i had .venv inside it , do i need to activate .venv ?

also when i create .venv does this create a requirements.txt file?



python3 -m venv .venv  ---> create environemnt 


source .venv/bin/actibate ---> activate the enviroenemnt 


python3 -m pip install pytest ---> now pytest is in your .venv 


so this command python3 -m pip freeze > requirements.txt

creates a text file called requirements auto 

you can also create it auto , with touch requirements.txt



also gitignore , when u create a environment u have gitignore inside the .venv file and you need to create one outside the .venv file 



.venv/
├── bin/
├── include/
├── lib/
├── .gitignore     ← this belongs to the venv
└── pyvenv.cfg




python-cli-auth-system/
├── .gitignore       ← create this one
├── .venv/
├── requirements.txt
├── practice.py
└── tests/




put this inside it 

.venv/
__pycache__/
.pytest_cache/
*.pyc
.env




