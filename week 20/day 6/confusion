#confusion 


def get_users(db: Session = Depends(get_db))
    


#so lets break this down!

#parameter called db 

# session is the type of object that db should hold 


#Depends(get_db) tells FastAPI how to obtain that Session object.

#execute() tells the SQLAlchemy Session to run a database command.


#what does depends() do ?


#“Get this value from another function instead of expecting the caller to pass it manually.”