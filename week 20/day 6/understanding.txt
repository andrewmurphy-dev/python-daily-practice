so here is some confusion when we get the sql query and then convert it to a python like object 





@router.get("")
def get_users(db: Session = Depends(get_db)):
    result = db.execute(text("SELECT id, name, password, created_at, FROM users"))
    rows = result.mapping().all()

    users = []

    for row in rows:
        users.append({
            "id": row["id"],
            "name": row["name"],
            "password": row["password"],
            "created_at": int(row["created_at"].timestamp()),
        })

    return users


so here we have a db parameter 
session  more precisely a type hint , db is expected to contain a SQLAlchemy Session object.
depends(get_db) this is  the expression and calling the get_db function 
The variable session is assigned the result of the expression Depends(get_db)


for example 

age: int = 29


age is a variable 
int type int 
29 value 

you should read it as 
db: Session

db should be a session 


db is the actual pointer (the variable holding the memory address).Session is a type hint (a label telling Python what kind of object that pointer is pointing to

               ┌── 1. The Pointer Name (Variable)
               │
               │   ┌── 2. The Type Hint (Label)
               │   │
               ▼   ▼
def get_users(db: Session = Depends(get_db)):



db: This is the Pointer Name. It is the variable that points to the database connection in memory.

Session: This is the Type Hint. It tells your code editor that the db pointer leads to a SQLAlchemy Session object.

Depends(get_db): This is the tool that actually builds the database connection object in memory and hands it to the db pointer.


so in the end 


THE POINTER (Variable Name)                 THE ACTUAL OBJECT (In Memory)
┌───────────┐                              ┌──────────────────────────────┐
│    db     │ ───────────────────────────> │  Session Object              │
└───────────┘    (Points to memory address)│  ├── Attributes (config)      │
                                           │  └── Methods                 │
                                           │       └── execute()          │
                                           └──────────────────────────────┘




Object (e.g., a Database Result, a String, or a Custom Class instance)
│
├── Data Components (State)
│   └── Attributes ───> Variables holding data specific to this object (e.g., user.id = 5)
│
├── Action Components (Behavior)
│   └── Methods ──────> Functions that act on this object's data (e.g., user.save())
│
└── Internal Mechanics (Metadata)
    ├── __class__ ────> Pointer to the blueprint/type that created this object
    ├── __doc__ ──────> The documentation string of the object
    └── Dunder Tools ─> Special hidden methods that make Python operators work:
                        ├── __init__ ──> Runs when the object is born
                        ├── __str__ ───> Controls what happens when you print(object)
                        └── __eq__ ────> Controls how object1 == object2 evaluates



confusion 



these are built in functions 

print("hello")
len("andrew")
type(123)
int("5")
str(99)

your own functions 

def say_hello():
    print("hello")



so i am confused ! 

so attributes and methods 



A method is an action attached to an object.
Some are built-in.
Some are made by developers.


for example

class User:
    def __init__(self, name, password):
        self.name = name
        self.password = password

    def login(self):
        print(f"{self.name} is logging in")