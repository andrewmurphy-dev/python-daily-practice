requests is a python package !

this lets your python code talk to websites , servers, and apis !


A request is a python library that builds and sends HTTP requests !

so when you do this



response + request.get(" https://pokeapi.co/api/v2/pokemon/pikachu")






so lets break this down clearly !


python creates a HTTP GET request

sends it across the internet to pokeapi.co (server)

waits for a respnse

stores the server rply in the variable resposne





the response object contains things like
response.status_code  # 200, 404, 500, etc.
response.text         # raw response body as text
response.json()       # convert JSON body into Python dict/list
response.headers      # metadata from the server





confusion

we have two layers !

1. request (this is importing python library)
this uses the library to create and send HTTP request !

import requests

response = request.get(url)


the request library wraps the response of the server into a python response object !

2.HTTP request
this is a structured message from a client to a server asking for something !





what is the point of get ?

this is "i want to retrieve read data from the server"



the server sends an http response ! this is called http response object !

this is

status code = did the request work ?
headers = metadata about the response
bodydata = the actual data /content returned !





for example !


HTTP/1.1 200 OK     ----> status code ! , 200 means success
Content-Type: application/json   ---> header
Server: cloudflare  ---> header


below is metadata
{
  "name": "pikachu",
  "height": 4,
  "weight": 60
}



for example



import requests


url = "https://pokeapi.co/api/v2/pokemon/pikachu"

response = request.get(url)

print(reponse.status_code)


data = response.json()

print(data["name"])


we set the meta data dictionary structure to a variable
so data = dict
and we search for the data !

