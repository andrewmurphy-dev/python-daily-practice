So the JSON from the API gets converted into Python data, then stored in the variable users.


where does this happen ?

response.json() 

takes the json body from the api response and converts it into normal python data 


so when we do this 

users = response.json()


means “convert the API JSON, and save it in a variable called users