what is result processing?
send SQL to the databse !



for example 

result = db.execute(text("SELECT * FROM menu_items"))

so db is the SQLalchmey session 


execute(...) 

ask the session to run something 


so the result is a variable holds SQLAlchemy way of accessing the data !

suppose the database finds these 

1, Burger, 10
2, Pizza, 12


now result reperesents those returned rows but not python format yet ! 


result = db.execute(text("SELECT * FROM menu_items))


1, Burger, 10
2, Pizza, 12





result = database query results
items  = results prepared for use in Python




so next;
here we use syntax 



items = result.mappings().all()




