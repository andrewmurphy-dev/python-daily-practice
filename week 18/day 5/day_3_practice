#day 3 practice 


from add.dp import db_test_connection 



def db_test_connection():
    with engine.connect() as connection:
        result = engine.execite(text("SELECT 1"))
        return result.scalar_one()
    


#this is wrong, 

connection.execute()

scalar_one()




def db_test_connection():
    with engine.connect() as connection:
        result = connection.execute(text("SELECT 1"))
        return result.scalar_one() 
    






@app.get("/db-test")
def db_test():
    result = {
        "connection": "successful",
        "database": result 
    }




#this is wrong 






@app.get("/db-test")
def db_test():
    result = db_test_connection()

    return {
        "database": "connected",
        "result": result
    }