#we need to make a health test in health for the db database to test to see if it works 


@router.get("/db-test")
def db_test():
    result = db_test_connection()

    return {
        "connection": "success",
        "result": result,
    }




#then we run the local host 

#uvicorn app.main_cli:app --reload 


